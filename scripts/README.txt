log4jdbc is built with Ant 1.10.x and JDK 1.8.x

The build.cmd file is useful for building both the JDBC 4 version
under Windows. The javadoc.cmd file is useful for building Javadoc.

NOTE: as of 2015-09-30 the jdbc 3 version has been removed.

